---
"@nicoflow/shared": minor
---

Add `detectTimezoneDrift`, `zoneOffsetMinutes`, and `formatZoneOffset` to `@nicoflow/shared/utils`, moved verbatim from `nicoflow-frontend`'s `src/features/Calendar/timezoneDrift.ts` so mobile can share the same device/account-timezone drift comparison logic (NIC-1946).
