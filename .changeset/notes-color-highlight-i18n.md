---
"@nicoflow/shared": patch
---

Add `textColorGroup`, `highlightGroup`, `colorDefault`, and 8 shared swatch-name keys (`colors.gray`/`red`/`orange`/`amber`/`green`/`teal`/`blue`/`purple`) to the `notes` i18n namespace (en/he/ru) — used by the new text-color and highlight toolbar pickers.
