---
"@nicoflow/shared": minor
---

Add `getBacklinks` query to `createNoteApi` (`GET /notes/:id/backlinks`) — powers the backlinks panel on the note page (NIC-1973), listing every note that mentions the given one.
