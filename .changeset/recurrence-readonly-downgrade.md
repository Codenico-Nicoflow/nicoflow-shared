---
'@nicoflow/shared': patch
---

Add `recurrence:badge.readOnly` and `recurrence:settings.readOnlyHint` locale keys (en/he/ru) for the graceful-downgrade read-only state on recurrence rules over the free plan's 3-rule cap.
