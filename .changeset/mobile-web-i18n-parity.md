---
"@nicoflow/shared": patch
---

Add missing i18n keys for mobile-only UI (task project picker, create/save error copy, task-actions menu label, FAB label, clear-date/clear-url button labels) so mobile and web keep resolving all task-form and field-clear copy through the same shared key set.
